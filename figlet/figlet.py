import pyfiglet
import random
import sys

if len(sys.argv) == 1:
    # Zero if the user would like to output text in a random font.
    sys.exit

try:
    len(sys.argv)
    #Two if the user would like to output text in a specific font, in which case the first of the two should be -f or --font, and the
except 
    ...
