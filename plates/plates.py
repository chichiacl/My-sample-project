def main():
    plate = input("Plate: ")
    if is_valid(plate):
        print("Valid")
    else:
        print("Invalid")


def is_valid(p):
       if 2 <= len(s) > 6:
        return False

def two_letter(s):
    s[0:2].isalpha()

def number(n):
    index = len(n) # 5
    return n[index].isdigit() # true


main()
